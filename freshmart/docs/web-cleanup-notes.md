# Web cleanup notes

Deferred on purpose during UI scope follow-up:

- Centralize isManager behind real auth/role source
- Normalize imports and formatting across web files
- Extract product alert badge logic into shared component
- Consider URL-driven view state for table/card toggle
